## TCP control

DPA N-Series supports OSC over both TCP and UDP. 
The module is preconfigured for TCP at the default port but can be changed to match a custom port set in your receiver's Advanced menu. 
You can also enable/disable OSC and set ports using DPA Audio Controller software on a computer. 
